import React from 'react';

const Product = ({ product }) => {
    const { name, price, image } = product;
    return (
        <div>
                      
        </div>
    );
};

export default Product;